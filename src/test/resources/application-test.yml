micronaut:
  application:
    name: desafioPixTestrest
  server:
    port: 8075

datasources:
  default:
    url: jdbc:h2:mem:carrosgrpctests;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: ""
    driverClassName: org.h2.Driver

# Habilita log do contexto transacional
logger:
  levels:
    io.micronaut.transaction: DEBUG